# 07 – Extension-Aware Middleware

Demonstrates how to inspect `scope->{extensions}` and send an extension-specific event (`http.fullflush`) only when the server advertises it.

Spec references: `docs/specs/main.mkdn` (extensions section) and `docs/extensions.mkdn` (fullflush example).
